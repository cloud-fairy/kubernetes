{{- $properties := (datasource "properties") -}}
{{- $project := (datasource "project") -}}

apiVersion: s3.aws.upbound.io/v1beta1
kind: Bucket
metadata:
  generateName: {{ $properties.bucket_name }}-crossplane-bucket
spec:
  forProvider:
    region: eu-central-1
  providerConfigRef:
    name: default